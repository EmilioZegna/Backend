<h1 class="titulo">Inicio</h1>
<h2 class="subtitulo">Nuestros Productos</h2>


{{#each productos}}
  <div class="card">
    <p> {{this.title}} </p>
    <p> {{this.description}} </p>
    <p> {{this.price}} </p>
  </div>
{{/each}}